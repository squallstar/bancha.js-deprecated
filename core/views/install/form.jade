h1 Install

form(method='post')
  .group
    label(for='f-hostname') Hostname
    input(id='f-hostname', type='text', placeholder='e.g. example.org, localhost', value=params.hostname, required)

  input(type='submit', value='Install')